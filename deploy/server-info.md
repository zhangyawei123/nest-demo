# 服务器连接信息

- 服务器 IP：`111.229.210.78`
- SSH 用户名：`root`
- SSH 密码：手动输入，不保存在本地文件中
- 数据库密码：手动输入，不保存在本地文件中

## 当前部署目录

- 后端目录：`/opt/nest-demo`
- 前端静态目录：`/var/www/nest-demo-frontend`

## 说明

- 后台新增功能部署到服务器时，需要同步前端页面和后端菜单初始化代码
- 若修改了菜单初始化逻辑，部署后需要执行一次：`POST /user/seed`
- 如果后续要给服务器数据库设置密码，由用户手动输入，并同步更新服务器上的 `.env`
